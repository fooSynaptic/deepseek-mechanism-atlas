% Step 3: Multi-head Hash Addressing (TikZ alternate path)
% Build: cd diagrams/reference && ./build_engram_01d.sh
\documentclass[tikz,border=6pt]{standalone}
\usepackage{amsmath,amssymb}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc,fit}

\begin{document}
\begin{tikzpicture}[
  font=\small,
  >=Stealth,
  arr/.style={-{Stealth[length=2mm]}, draw=slate!70!black, thick},
  box/.style={draw, rounded corners=3pt, align=center, inner sep=4pt},
  hashbox/.style={box, fill=orange!12, draw=orange!70!black},
  idxbox/.style={box, fill=orange!6, draw=orange!50!black, minimum width=11mm},
  embbox/.style={box, fill=green!10, draw=green!50!black, minimum width=12mm},
  tblbox/.style={box, fill=violet!8, draw=violet!60!black, minimum width=22mm, minimum height=7mm},
  notebox/.style={box, fill=green!6, draw=green!40!black},
  infobox/.style={box, fill=blue!6, draw=blue!40!black, font=\scriptsize},
]

\node[font=\bfseries] at (9,5.6) {Step 3: Multi-head Hash Addressing};
\node[font=\scriptsize, text=slate] at (9,5.15) {%
  $z_{t,n,k}=\varphi_{n,k}(g_{t,n}),\quad e_{t,n,k}=E_{n,k}[z_{t,n,k}]$
  \quad|\quad deterministic multiply-XOR hash};

\node[box, fill=slate!8, draw=slate!50, minimum width=20mm] (gin) at (1.4,3.2) {%
  $g_{t,n}$\\[-1pt]{\scriptsize n-gram at $t$}};

\node[infobox, minimum width=20mm] at (1.4,1.6) {%
  prime-sized tables\\per head\\multi-head lowers collisions};

\coordinate (split) at (2.8,3.2);
\draw[arr] (gin.east) -- (split);
\draw[arr] (split) |- (3.2,4.5);
\draw[arr] (split) -- (3.2,3.2);
\draw[arr] (split) |- (3.2,1.9);
\node[font=\scriptsize, text=slate] at (2.55,3.55) {$K$ heads};

\foreach \k/\y in {1/4.5, 2/3.2, K/1.9} {
  \node[hashbox, minimum width=16mm] (phi\k) at (4.2,\y) {%
    $\varphi_{n,\k}$\\[-2pt]{\scriptsize hash head \k}};
  \node[idxbox] (z\k) at (6.0,\y) {$z_{t,n,\k}$};
  \node[tblbox] (E\k) at (8.0,\y) {$E_{n,\k}$};
  \node[embbox] (e\k) at (10.0,\y) {$e_{t,n,\k}$};
  \draw[arr] (3.2,\y) -- (phi\k.west);
  \draw[arr] (phi\k) -- (z\k);
  \draw[arr] (z\k) -- (E\k);
  \draw[arr] (E\k) -- (e\k);
  \fill[violet!70] ($(E\k.center)+(-1.5mm,-0.8mm)$) rectangle ++(3mm,1.6mm);
}

\node at (4.2,2.55) {$\vdots$};

\coordinate (merge) at (11.0,3.2);
\draw[arr] (e1.east) -| (merge);
\draw[arr] (e2.east) -- (merge);
\draw[arr] (eK.east) -| (merge);
\node[box, fill=violet!12, draw=violet!70, minimum width=16mm] (concat) at (12.2,3.2) {%
  concat\\[-1pt]{\scriptsize Step 4: $e_t$}};
\draw[arr] (merge) -- (concat.west);

\node[notebox, minimum width=38mm, align=center] at (12.2,1.3) {%
  \textbf{Pre-calculable}\\[-1pt]
  {\scriptsize index depends only on input tokens}\\
  {\scriptsize prefetch / offload before GPU forward}};

\node[font=\scriptsize, text=slate] at (9,0.35) {%
  each order $n$ has $K$ independent heads;
  full n-gram space too large for explicit tabulation};

\end{tikzpicture}
\end{document}
